import React from 'react';

const Loader = () => {
    return(
        <div className='Loader'>
            <img src="/img/loader.svg" alt="" width={500}/>
        </div>
    );
};

export default Loader